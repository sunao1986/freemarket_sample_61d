.single-container
  %header.single-container__header
    %h1
      = link_to "https://www.mercari.com/jp/" do
        = image_tag "//www-mercari-jp.akamaized.net/assets/img/common/common/logo.svg?465897195", alt: "mercari", size: "193x49"
    %nav.progress-bar.signup-bar
      %ol
        %li.active
          %p 会員情報
          .progress-status
        %li
          %p 電話番号認証
          .procress-status
        %li
          %p お届け先住所入力
          .procress-status
        %li
          %p 支払い方法
          .procress-status
        %li
          %p 完了
          .procress-status  
  %main.single-container__main
    %section.l-single-container
      %h2.l-single-container__head.registration 会員情報入力
      = form_tag("/", method: :post, class: "l-single-inner registration-form" ) do
        .l-single-container__content
          .form-group
            = label_tag "ニックネーム" 
            %span.form-require 必須
            = text_field :name, "", class: "input-default", placeholder: "例) メルカリ太郎"
          .form-group
            = label_tag "メールアドレス" 
            %span.form-require 必須
            = email_field :name, "", class: "input-default", placeholder: "PC・携帯どちらでも可"
            = hidden_field :name, ""
          .form-group
            = label_tag "パスワード" 
            %span.form-require 必須
            = password_field :name, "", class: "input-default", placeholder: "7文字以上"
          .form-group
            = label_tag "パスワード（確認）" 
            %span.form-require 必須
            = password_field :name, "", class: "input-default", placeholder: "7文字以上"  
          .form-group
            %h3.text-left.l-chapter-sub-head  本人確認
            %p.l-single-text
              安心・安全にご利用いただくために、お客様の本人情報の登録にご協力ください。他のお客様に公開されることはありません。
          .form-group
            %div
              = label_tag "お名前(全角)"    
              %span.form-require 必須
              = text_field :name, "", class: "input-default half", placeholder: "例) 山田"
              = text_field :name, "", class: "input-default half", placeholder: "例) 彩"
          .form-group    
            %div
              = label_tag "お名前カナ(全角)"    
              %span.form-require 必須
              = text_field :name, "", class: "input-default half", placeholder: "例) ヤマダ"
              = text_field :name, "", class: "input-default half", placeholder: "例) アヤ"
          .form-group
            %div
              = label_tag "生年月日" 
              %span.form-require 必須
              %br
              .birthday-select-wrap
                .select-wrap
                  %i.icon-arrow-bottom
                  %select.select-default
                   
                    -# = date_select :birthday, start_year: 1940, end_year: Time.current.year, use_month_numbers: true

= render partial: 'shared/header'
- breadcrumb :profile
= render partial: 'shared/gretel'
.mypage-container-profile
  .pro-container
    = form_for @user do |f|
      .profile-head
        %h1 プロフィール
      .profile-user
        .profile-user__box
          .profile-user__box__icon
            - if @user.image?
              = f.label :image do
                = image_tag @user.image.url, width:"60px", height:"60px", class: "profile-icon"
                = f.file_field :image ,class:"input-image"
            - else
              = f.label :image do
                = image_tag("user-image.png", width:"60px", height:"60px", class: "profile-icon")
                = f.file_field :image ,class:"input-image"
          .profile-user__box__user
            = f.text_area :nickname, type:"text", placeholder:"例）AYA☆セール中" ,class:"input-nickname"
      .profile-container
        = f.text_area :introduction, cols:"34", rows:"4", placeholder:"例）こんにちは☆ ご覧いただきありがとうございます！かわいいものやきれいめオフィスカジュアル中心に出品しています。服のサイズは主に9号です。気持ちよくお取引できるよう心がけていますので、商品や発送方法などご質問がありましたらお気軽にどうぞ♪", class:"input-profile"
        .profile-container__editbtn
          = f.submit "変更する", class:"editbtn"
  = render "shared/sidebar"
= render 'shared/sell'
= render 'shared/aside_and_footer'